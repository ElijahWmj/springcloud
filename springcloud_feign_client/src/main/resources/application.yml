spring:
  application:
    name: springcloud-feign-client
server:
  port: 9091
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

feign:
  hystrix:
      enabled: true
