server:
  port: 8799

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/chenyn-java/springcloud-config.git
          basedir: F:/git


